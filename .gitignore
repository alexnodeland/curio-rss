# Rust / Cargo (workspace root)
/target/

# Desktop head (apps/desktop — outside the cargo workspace until Phase 4)
node_modules/
apps/desktop/.svelte-kit/
apps/desktop/build/
apps/desktop/dist/
apps/desktop/coverage/
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/bin/
apps/desktop/src-tauri/gen/

# Curio runtime state — the events log is never committed (curio.events.v1)
.curio/events/

# Generated test fixtures — rebuilt via `just fixtures`, never committed
/fixtures/generated/

# Nightly cold-start bench artifacts — produced in CI, cached (not committed)
/.bench-cache/
/bench-output.txt

# Coverage
coverage/
lcov.info

# Environment
.env
.env.*
!.env.example

# IDE
.idea/
*.swp
*.swo
*~

# System files
.DS_Store
Thumbs.db
