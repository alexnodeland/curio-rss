{
    "name": "curio-desktop-e2e",
    "private": true,
    "version": "0.1.0",
    "description": "WebdriverIO + tauri-driver smoke suite for the Curio desktop head (Linux + Windows; there is no macOS tauri-driver). Self-contained: installed only by the nightly smoke job, never by the per-PR gates.",
    "type": "module",
    "scripts": {
        "test": "wdio run ./wdio.conf.ts"
    },
    "devDependencies": {
        "@wdio/cli": "^9.0.0",
        "@wdio/local-runner": "^9.0.0",
        "@wdio/mocha-framework": "^9.0.0",
        "@wdio/spec-reporter": "^9.0.0",
        "tsx": "^4.7.0",
        "typescript": "^5.3.0",
        "webdriverio": "^9.0.0"
    }
}
