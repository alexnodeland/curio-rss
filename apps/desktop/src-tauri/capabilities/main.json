{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The single main window. Minimal ACL: IPC core, native file dialogs, and a URL-scoped opener for open-in-browser. No shell plugin, no fs plugin — all file IO happens in Rust commands behind the ipc_policy path gate.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }]
    }
  ]
}
